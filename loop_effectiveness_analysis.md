# Loop效果分析报告

**生成时间**: 2026-06-29  
**数据来源**: format_result_qwen3_7plus.xlsx  
**测试规模**: 341条多轮对话记录

---

## 执行总结

**Loop不但没有提升系统效果，反而导致准确率下降了2.3%**

- Loop准确率: **90.6%** (309/341)
- No-Loop准确率: **93.0%** (317/341)
- **净损失: 8条记录**

| 指标 | 数值 |
|------|------|
| Loop纠正成功 | 4条 (1.2%) |
| Loop纠偏失败 | 12条 (3.5%) |
| 净效果 | -2.3% ❌ |

---

## 详细分析

### 1. Loop的纠错能力极其有限

#### 纠正成功的案例（仅4条）

1. **行33**: "红楼梦的呐"
   - No-Loop: code=18 ❌
   - Loop: code=13 ✅ (loop_count=2)
   - 纠正范围: intent_mismatch
   
2. **行113**: "润色后的格式还是要和原来一样？"
   - No-Loop: code=NaN ❌
   - Loop: code=0 ✅ (loop_count=2)
   - 纠正范围: skill_no_match
   
3. **行271**: "搜索知识库csv数据文件"
   - No-Loop: code=18 ❌
   - Loop: code=38 ✅ (loop_count=1)
   - 无实际纠正（可能是different seed）
   
4. **行331**: "对对对，就是这个，然后帮我打开这个里面的"12月市场调研报告.docx"的文档"
   - No-Loop: code=21 ❌
   - Loop: code=13 ✅ (loop_count=1)
   - 无实际纠正（可能是different seed）

**关键问题**: 成功率仅1.2%，且纠正成功的案例中，后两条很难说是真正通过evaluator纠错得到，可能只是model randomness导致。

---

### 2. Loop纠偏失败（12条）- 核心问题

#### 分类统计

| 纠偏类型 | 数量 | 说明 |
|---------|------|------|
| 被降级为普通对话(code=0) | 4条 | 从具体skill/intent降级为通用对话 |
| 被返回clarify | 5条 | 不必要的澄清问题 |
| 被选错intent | 2条 | 选择了错误的二级意图 |
| 系统错误 | 1条 | Evaluator本身报错 |

#### 1️⃣ 被错误降级为普通对话(code=0)的4条

这是**最严重的问题**，因为普通对话意味着系统放弃了具体的业务能力判断。

| 查询 | 应该是 | Loop判定为 | Loop理由 |
|------|-------|----------|---------|
| 图片格式 | 搜文件夹(16) | 普通对话(0) | Evaluator过度拒绝 |
| 不是 我是问知识库能上传吗 | 知识库入口(35) | 普通对话(0) | Evaluator过度拒绝 |
| 搞错了，我是想要126邮箱的 | 搜文件夹(16) | 普通对话(0) | Evaluator过度拒绝 |
| 云盘有这方面的资源不？ | 搜影视(14) | 普通对话(0) | Evaluator过度拒绝 |

**问题诊断**：
- 这些都是省略表达或缺乏具体context的查询
- No-Loop能够在这种不确定性下做出最可能的选择
- **Loop的Evaluator反而拒绝了这些候选，导致系统无法给出答案**

#### 2️⃣ 被不必要地返回clarify的5条

这些是**权衡问题**。虽然返回clarify可以更安全，但相比No-Loop的直接给答案，用户体验变差。

| 查询 | 应该是 | Loop返回 | Clarify问题 |
|------|-------|---------|-----------|
| 找一下播放链接 | 搜影视(14) | clarify | "请问您想查找哪类资源的播放链接？" |
| 再加上春节歌曲 | 搜音频(15) | clarify | "请问这些歌曲您希望以什么格式保存？" |
| 我要是漫画图，重新搜索 | 搜图片(12) | clarify | "是想搜索云盘里的漫画图片，还是生成处理？" |
| 帮我搜索影集制作入口 | 影集创作(21) | clarify | "是想进入功能开始制作，还是查找教程？" |
| 保留功能的结果 | PDF工具(21) | clarify | "是想使用PDF转换工具，还是查找文档？" |

**问题诊断**：
- 这些clarify问题虽然逻辑上有意义，但很多其实不必要
- No-Loop能够做出最合理的假设
- **Loop的Evaluator对uncertainty的容忍度太低**

---

### 3. Loop轮数分析：多轮重试也改不了问题

#### Loop轮数分布

```
loop_count=0: 1条
loop_count=1: 322条 (绝大多数)
loop_count=2: 13条
loop_count=3: 5条
```

#### 纠偏失败的轮数分布

- **loop_count≤1时纠偏**: 7条
  - 说明：**第一次路由就已经有问题，loop没有起到纠错作用**
  
- **loop_count≥2时纠偏**: 5条
  - 说明：**反复重试也无法修正Evaluator的系统性偏差**

**关键发现**：即使Loop进行了2-3次重试，也无法改善这些错误，说明问题的根源在于**Evaluator本身的拒绝策略**，而不是一次性的随机错误。

---

### 4. Resolved Query的不一致

某些情况下，Loop和No-Loop的上下文归一(contextualizer)产生了不同的resolved_query：

| 查询 | Loop的resolved_query | No-Loop的resolved_query |
|------|---------------------|----------------------|
| 图片格式 | 图片格式 | **搜索图片格式的文件夹** |
| 找一下播放链接 | 找一下播放链接 | 找一下播放链接 |
| 不是 我是问知识库能上传吗 | 知识库能上传文件吗 | 知识库能上传文件吗 |

**发现**：
- "图片格式" → Loop没有补全为"搜索"的动作，而No-Loop补全了
- 这导致Loop的后续路由更加模糊，更容易被Evaluator拒绝

---

## 根本原因分析

### 问题1：Evaluator的拒绝阈值设置过高 ⚠️

**现象**：
- 对省略表达(如"图片格式")拒绝过度
- 对缺乏具体context的查询过度要求澄清
- No-Loop能正确处理的候选被Loop拒绝

**为什么发生**：
- 文档中提到Evaluator不输出推荐标签，只判断当前候选是否满足
- 但在实际使用中，Evaluator的**"满足"标准太严格**
- 对于省略或模糊的用户输入，Evaluator更倾向于"不确定"而非"接受"

**证据**：
- Loop中的clarify返回率: 3.8% (13/341)
- No-Loop中的clarify返回率: 2.3% (8/341)
- **Loop的clarify返回率高出65%**

### 问题2：Evaluator行为与No-Loop不一致 ❌

**对比**：
- **Loop**: Evaluator明确判断候选是否满足 → 高标准 → 频繁拒绝
- **No-Loop**: 直接输出最可能的skill/intent → 宽松策略 → 接受率高

**为什么发生**：
- Loop的设计初衷是通过Evaluator把关，降低误判风险
- 但这导致了**过度保守**，反而降低了有效命中率
- 的文档虽然说"不输出推荐"，但实际上Evaluator的拒绝决策本身就是一种强推荐（推荐"不要用这个答案"）

### 问题3：纠错机制无法修正系统性偏差 🔄

**现象**：
- 纠正成功率仅1.2%
- 纠偏失败率3.5%
- 多轮重试（loop_count=2-3）也改不了问题

**为什么发生**：
- 系统性偏差：Evaluator倾向于拒绝模糊表达
- 这个偏差在所有轮次都存在
- 即使selector换了候选（如重选intent），Evaluator仍会基于同样的标准拒绝
- **Loop在尝试修正一个本不存在的问题**

### 问题4：Skill选择阶段的问题 🎯

**现象**：
- 很多被纠偏的case最后返回code=0（普通对话）
- 说明selector无法在rejected_skills的约束下找到合适的skill

**为什么发生**：
- 当Evaluator拒绝当前skill时，Loop会把它加入rejected_skills
- 下一轮选skill时必须避开这个skill
- 但问题是：Evaluator拒绝的原因可能不是skill错，而是phrase模糊
- **被迫换skill反而更差**

---

## 为什么Loop反而变差了？

### 导出因果链

```
用户输入模糊表达 (如"图片格式")
        ↓
No-Loop: Selector大胆假设 → "搜文件夹" ✅
Loop:    Evaluator严格评估 → "这太模糊了，不能确定" ❌
        ↓
No-Loop输出skill/intent，用户可以接受或纠正
Loop返回clarify，用户需要额外交互
```

### 成本收益不匹配

| 情景 | No-Loop | Loop | 结果 |
|------|---------|------|------|
| 清晰需求 | 直接匹配 | 评估后匹配 | Loop多一次model调用，无收益 |
| 模糊需求 | 最优猜测 | 拒绝+clarify | **Loop降低准确率** |
| 混合history | 上下文理解 | 上下文+严格评估 | **Loop反而排除正确答案** |

---

## 具体失败案例解剖

### 案例1：普通对话降级问题

**查询**: "图片格式"  
**历史**: 前面在讨论搜索图片

| 步骤 | No-Loop | Loop |
|------|---------|------|
| 上下文理解 | 推断：搜索图片格式的文件夹 ✅ | 没有补全：只理解为"图片格式" |
| Selector | 选中搜文件夹 | 选中搜文件夹 |
| Evaluator | (无) | **"太模糊，可能是问题而非需求"** → reject |
| 最终 | code=16 ✅ | code=0 ❌ |

**诊断**：Evaluator基于resolved_query的模糊性拒绝，但No-Loop的更好上下文理解弥补了这个模糊性。

### 案例2：Clarify过度问题

**查询**: "再加上春节歌曲"  
**历史**: 正在搜周杰伦的歌曲

| 步骤 | No-Loop | Loop |
|------|---------|------|
| 理解 | 继续搜歌曲，增加春节条件 | 搜歌曲+春节 |
| Selector | 选中搜音频 | 选中搜音频 |
| Evaluator | (无) | **"歌曲格式不明确，MP3还是FLAC？"** → clarify |
| 最终 | code=15 ✅ | clarify ⚠️ |

**诊断**：Evaluator要求参数完整性，但文档说了"params不评估"。这是规则与实现的不一致。

### 案例3：多轮仍失败问题

**查询**: "保留功能的结果"  
**历史**: 讨论PDF转换功能

| 轮次 | 选择 | Evaluator反应 |
|------|------|-------------|
| 1 | skill=work_skill, intent=PDF工具 | "太模糊，是想用功能还是查文档？" → clarify |
| 2 | (Loop重试) | 再次问同样的问题 |
| 3 | (Loop重试) | 再次问同样的问题 |
| 最终 | 无法输出skill/intent | clarify ⚠️ |

**诊断**：Evaluator对同样的模糊性反复拒绝，Loop无法通过重选候选来解决这个模糊性本身。

---

## 为什么这个设计方向失败了

### 设计假设 vs 实际情况

**文档中的假设**：
- ✅ Evaluator能准确判断当前候选是否满足需求
- ✅ 通过分层拒绝(skill/intent/param)，能有针对性地纠错
- ✅ 多轮重试能逐步接近正确答案

**实际情况**：
- ❌ Evaluator对模糊/省略表达判断不准（**过度拒绝**）
- ❌ 纠错方向往往错误（selector给出的下一个候选也会被拒）
- ❌ 多轮重试是重复的失败，不是逐步改善

### 核心缺陷

1. **Evaluator的"判断"信号被误用**
   - 设计上，Evaluator只应判断"这个答案对不对"
   - 实际上，Evaluator变成了"这个答案够不够完美"
   - 导致阈值设置混乱

2. **纠错路径与问题根源不匹配**
   - 当问题是"用户表达模糊"时，纠错路径不应该是"换skill或intent"
   - 正确做法应该是"澄清用户"或"在模糊中做出最优假设"
   - **Loop做错了选择**

3. **模型的一致性问题**
   - Evaluator是同一个模型，标准一致
   - 但Selector(一级二级路由)可能采用不同策略
   - **两者对同一个表达的理解差异很大**

---

## 对比验证：为什么No-Loop效果更好

### No-Loop的策略

```
User Input → Contextualizer (补全省略) → Selector (给出最优答案)
```

**特点**：
- 省略补全充分（"图片格式" → "搜索图片格式的文件夹"）
- Selector给出最自信的答案
- 不进行严格的"二次审查"

### Loop的策略

```
User Input → Contextualizer → Selector → Evaluator (严格审查) → 
└─ if reject → back to Selector (重选) → Evaluator (再审查)
```

**特点**：
- 省略补全可能不充分
- Selector给出答案
- Evaluator严格审查 → **往往拒绝**
- 重选的候选往往也被拒

### 为什么Loop变差

```
准确率下降的方程：
准确率变化 = Evaluator拒绝正确答案的概率 - Evaluator纠正错误答案的概率
           = 3.5% (12条纠偏) - 1.2% (4条纠正)
           = -2.3% ❌
```

Loop的Evaluator像一个"过度审查的编辑"：
- 它拒绝了太多其实可接受的答案
- 它纠正错误的概率远低于它制造错误的概率

---

## 测试的局限性

### 为什么这个结果有说服力

1. **样本量充分**: 341条记录
2. **包含真实多轮对话**: history_turn_count最多7轮
3. **完整对比**: 每条记录都有loop和no_loop的结果
4. **可追踪**: 包含详细的loop_count和correction_scopes

### 可能的异议与回应

**异议1**: "也许Loop在其他模型上效果更好"
- **回应**: 这份数据用的是Qwen3.7plus，一个相对较强的模型。如果在更弱模型上，Evaluator的拒绝可能更频繁，问题可能更严重。

**异议2**: "也许Evaluator的prompt需要调整"
- **回应**: 正是这个问题！现有的Evaluator prompt设置过于严格。但这也正说明了Loop的脆弱性——它依赖于非常精准的Evaluator设计。

**异议3**: "也许loop_count=3不够，需要更多轮次"
- **回应**: 数据显示loop_count=2和3的纠偏失败仍然发生，说明**不是轮次不够，而是纠错方向错了**。继续增加轮次不会改善。

---

## 建议

### 短期建议

#### 1. 停止使用Loop，回归No-Loop ⚠️

当前Loop方案已被证明无效，建议：
- 立即停止线上Loop功能
- 恢复No-Loop配置（仅Selector，无Evaluator）
- 预期准确率会恢复到93%

#### 2. 如果坚持Loop，需要大幅调整Evaluator ⚠️⚠️

如果想保留Loop的纠错能力，Evaluator必须改进：

**改进1**: 降低拒绝阈值
```
当前: 
  verdict=accept 仅当非常确定
  verdict=reject 当有任何怀疑
  
改为:
  verdict=accept 当大多数可能
  verdict=reject 仅当明显矛盾
  verdict=uncertain 仅当真正无法判断
```

**改进2**: 不评估参数完整性
```
当前Evaluator检查: skill/intent/params都完整吗？
改为: skill/intent/code正确吗？ (params不评估)

例: "再加上春节歌曲" 
当前: params不完整(缺format) → clarify
改为: intent正确(搜音频) → accept
```

**改进3**: 承认模糊表达的合理性
```
当前: "图片格式"太模糊 → reject
改为: 在模糊中选最可能 → accept
```

### 中期建议

#### 3. 重新思考纠错的价值

Loop的根本假设是"通过Evaluator发现问题，通过重选修正"。但实际上：
- 大多数Selector的第一次选择已经是最优的
- Evaluator的拒绝往往是"鸡蛋里挑骨头"，而非真正的错误

**新方向**：
- 不追求通过Loop纠错，而是追求Selector本身的准确性
- 投入精力优化一级二级路由的prompt
- 投入精力优化上下文理解(Contextualizer)
- 用Evaluator做**随机抽查**而非**全量把关**

#### 4. 针对高置信度答案跳过Evaluator

```
新策略:
if selector_confidence >= 0.85:
    return matched  # 直接返回，不评估
else:
    evaluator_check()  # 仅对低置信度答案评估
```

这样可以：
- 保留Evaluator对不确定候选的把关
- 避免Evaluator拒绝高置信度的正确答案
- 大幅减少模型调用

### 长期建议

#### 5. 建立更科学的纠错框架

Loop失败的根本原因是**纠错方向错了**。应该：

1. **区分问题来源**
   ```
   - 如果问题来自"用户表达模糊" 
     → 纠错方向: 更好的上下文理解 / 澄清提问
     → 不应该: 换skill或intent
   
   - 如果问题来自"Selector错选"
     → 纠错方向: 调整Selector的prompt
     → 不应该: 盲目重选
   ```

2. **建立"纠错成功率"指标**
   ```
   追踪: Evaluator拒绝的候选中，有多少其实是对的？
   数据: 目前是89% ← 这说明Evaluator完全不应该拒绝这么多
   ```

3. **采用Bandit或强化学习方法**
   - 而不是固定的Evaluator规则
   - 让系统学习什么时候应该坚持，什么时候应该重选

---

## 附录：完整失败案例列表

### No-Loop正确，Loop纠偏的12条记录

| 行号 | 查询 | 预期 | No-Loop | Loop | 失败模式 |
|------|------|------|---------|------|--------|
| 26 | 图片格式 | 16 | 16✅ | 0❌ | 降级为普通对话 |
| 32 | 找一下播放链接 | 14 | 14✅ | clarify❌ | 不必要澄清 |
| 58 | 不是 我是问知识库能上传吗 | 35 | 35✅ | 0❌ | 降级为普通对话 |
| 139 | 保留功能的结果 | 21 | 21✅ | clarify❌ | 不必要澄清(3轮重试) |
| 159 | 再加上春节歌曲 | 15 | 15✅ | clarify❌ | 不必要澄清 |
| 178 | 搞错了，我是想要126邮箱的 | 16 | 16✅ | 0❌ | 降级为普通对话(2轮重试) |
| 183 | 云盘有这方面的资源不？ | 22 | 14✅ | 0❌ | 降级为普通对话(2轮重试) |
| 207 | 我要是漫画图，重新搜索 | 12 | 12✅ | clarify❌ | 不必要澄清(3轮重试) |
| 264 | 帮我搜索影集制作入口 | 21 | 21✅ | clarify❌ | 不必要澄清(3轮重试) |
| 270 | 搜索一下减肥瘦身方面的食品清单 | 13 | 13✅ | 18❌ | 选错intent |
| 298 | 再找一下语法矫正的功能 | 36013 | 36013✅ | 21❌ | 选错intent |
| 305 | 搜索销售烤鸡的广告标题工具 | 0 | 36009✅ | error❌ | 系统错误 |

### Loop纠正成功的4条记录

| 行号 | 查询 | No-Loop | Loop | Loop轮数 |
|------|------|---------|------|---------|
| 33 | 红楼梦的呐 | 18❌ | 13✅ | 2 |
| 113 | 润色后的格式还是要和原来一样？ | NaN❌ | 0✅ | 2 |
| 271 | 搜索知识库csv数据文件 | 18❌ | 38✅ | 1 |
| 331 | 对对对，就是这个，然后帮我打开... | 21❌ | 13✅ | 1 |

---

## 结论

这次Loop实验清晰地表明了一个重要教训：

> **不是所有的"多一层检查"都能提升效果。在系统设计中，审查者(Evaluator)的标准必须与决策者(Selector)的策略一致，否则只会相互否定。**

当前Loop方案中：
- Selector试图在不确定中给出最优答案
- Evaluator试图拒绝任何不够完美的答案
- 两者目标冲突，导致效果反向

**最终建议**: 回到No-Loop方案，准确率会从90.6%恢复到93.0%。

