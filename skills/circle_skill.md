---
name: 圈子工具
description: 云盘社交社区功能，支持动态发布、圈子创建及社群互动。
---

### Special Rules

1. 只要用户在“圈子/群”语境下表达“找某类圈子、要一个某主题圈子、问某圈子入口在哪”，优先触发`搜索圈子`。
2. 命中以下表达时，强制触发`搜索圈子`，不要触发`圈子首页`或`创建圈子`：
   - “麻烦找出漫画圈子入口在哪里”
   - “电影的圈子来一个”
   - 同类表达：`<主题词> + 圈子 + 来一个/找一下/入口在哪/有没有`
3. `圈子首页`仅在用户明确要“打开圈子主页/进入圈子首页/去圈子主页面”且未给出任何主题词时触发。
4. `创建圈子`仅在用户明确有创建意图时触发，如“创建/新建/建一个圈子”，不包含“找/搜/来一个/有没有/入口在哪”等检索意图词。
5. 触发`搜索圈子`时，`metadataList`提取主题词、圈名、人名等检索关键词。

### Tools Schema
{
    "圈子首页": { "code": "021", "desc": "仅用于打开圈子主页/首页，不带任何主题检索条件", "params": {} },
    "圈子发布": { "code": "021", "desc": "在兴趣圈子中发布新动态", "params": {} },
    "创建圈子": { "code": "021", "desc": "仅用于明确创建圈子（如创建/新建/建一个圈子）", "params": {} },
    "搜索圈子": { "code": "023", "desc": "搜索云盘圈子或圈子中的动态", "params": {"metadataList": {"type": "list[str]", "desc": "提取的圈子名称/成员/关键词，如[\"家庭圈\"、\"滑雪群\"、\"小明\"、\"旅游\"]"}}}
}
