# 分层意图识别项目架构设计

> 版本：v2.0.0 | 日期：2026-06-29

---

## 一、项目概述

移动云盘意图路由系统，将用户的自然语言输入映射为结构化的路由决策（skill / intent / code / params）。系统接入 15 类云盘能力，通过多阶段候选集生成与择优机制完成路由。

---

## 二、总体架构

### 2.1 核心链路

```
用户输入
  │
  ▼
┌─────────────────────────────────────────────────────┐
│  Contextualizer（上下文语义归一）                      │
│  输入: current_user_query + dialogue_history          │
│  输出: resolved_query + semantic_frame                │
└─────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────┐
│  Skill Candidate Generator（一级候选召回）             │
│  输入: resolved_query + 15类skill cards               │
│  输出: Top-N SkillCandidate[] (默认 N=3)              │
└─────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────┐
│  Intent Candidate Generator（二级候选召回）             │
│  输入: resolved_query + 每个skill的完整MD              │
│  输出: IntentCandidate[] (合并去重)                    │
└─────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────┐
│  Evaluator Reranker（候选集重排序）                    │
│  输入: resolved_query + candidate set                 │
│  输出: RerankDecision { select | expand }             │
└─────────────────────────────────────────────────────┘
  │
  ├── verdict=select ──► Switch Gate ──► RouteResult
  │
  └── verdict=expand ──► 定向扩充 ──► 回到 Skill/Intent 召回 ──► 重新 Rerank
```

### 2.2 模块职责总览

| 模块 | 文件 | 职责 |
| --- | --- | --- |
| `SkillRegistry` | `skills.py` | 加载 `skills/*.md`，解析为结构化能力定义 |
| `Contextualizer` | `router.py:_contextualize` | 多轮对话语义归一 |
| `Skill Candidate Generator` | `router.py:_generate_skill_candidates` | 从15类能力中召回Top-N候选 |
| `Intent Candidate Generator` | `router.py:_generate_intent_candidates` | 在每个skill内召回Top-M intent候选 |
| `Evaluator Reranker` | `router.py:_rerank_candidates` | 候选集内选择最优或请求扩充 |
| `Switch Gate` | `router.py:_apply_switch_gate` | 安全门控，校验evaluator切换推荐 |
| `Targeted Expansion` | `router.py:_expand_candidates` | 定向补充漏召回候选 |
| `Validation` | `validation.py` | 本地硬校验（skill_id存在性、intent/code合法性） |
| `IntentDialogueAgent` | `dialogue.py` | 有状态多轮对话管理器 |
| `Model Client` | `model_client.py` | AgentScope模型调用适配层 |

---

## 三、核心模块详解

### 3.1 SkillRegistry — 能力注册表

**输入**：`skills/` 目录下的 Markdown 文件

**Skill 文件结构**（以 `mcloud_search_skill.md` 为例）：

```markdown
---
name: 云盘搜索
description: 当用户意图是查找、定位、获取资源载体本身时...
---

### Special Rules
1. 资源目标 vs 答案目标：...
2. 检索主动作 vs 非检索主动作：...

### Tools Schema
{
    "搜图片": {"code": "012", "desc": "搜索图片", "params": {...}},
    "搜文档": {"code": "013", "desc": "搜索文档", "params": {...}},
    ...
}
```

**解析逻辑**（`skills.py:load_skill`）：

1. 解析 YAML Frontmatter 获取 `name`、`description`
2. 提取 `### Special Rules` 章节作为业务规则
3. 提取 `### Tools Schema` 章节，解析 JSON 得到 `{intent_name: {code, desc, params}}`
4. 构造 `SkillDefinition`，包含完整 Markdown 原文

**输出**：`SkillDefinition` 和 `SkillCard`（轻量摘要，用于一级路由）

```python
class SkillDefinition:
    id: str                    # 如 "mcloud_search_skill"
    name: str                  # 如 "云盘搜索"
    description: str
    special_rules: str         # Special Rules 原文
    raw_markdown: str          # 完整 Markdown 原文
    intents: dict[str, IntentSchema]  # intent_name → {code, desc, params}

class SkillCard:               # 轻量卡片，发给一级路由模型
    id: str
    name: str
    description: str
    intents: list[str]         # intent 名称列表
```

### 3.2 Contextualizer — 上下文语义归一

**触发条件**：`dialogue_history.turns` 非空时调用模型；无历史时直接透传原始 query。

**输入**：
```json
{
    "current_user_query": "最近的",
    "dialogue_history": [
        {
            "turn_index": 0,
            "user_query": "帮我找猫",
            "semantic_state": {"status": "matched", "resolved_query": "搜索猫图片"},
            "assistant_result": {"status": "matched", "skill_id": "mcloud_search_skill", "intent": "搜图片", "code": "012"}
        }
    ]
}
```

**输出**：`ContextualizedRequest`
```python
class ContextualizedRequest:
    resolved_query: str              # "搜索最近的猫图片"
    relation_to_history: Literal[    # "continuation"
        "new_request", "continuation", "revision",
        "answer_to_previous", "ambiguous"
    ]
    semantic_frame: SemanticFrame    # 结构化语义帧
    reason: str                      # 归一推理

class SemanticFrame:
    action: str | None               # "搜索"
    expected_result_type: Literal[   # "resource"
        "resource", "function_entry", "content_generation",
        "content_processing", "mail_action", "social_share",
        "ordinary_answer", "unknown"
    ]
    object_types: list[str]          # ["图片"]
    subjects: list[str]              # ["猫"]
    qualifiers: list[str]            # []
    inherited_turns: list[int]       # [1]
    explicit_overrides: list[str]    # []
    uncertainty_notes: list[str]     # []
```

**关键约束**（prompt 中明确要求）：
- 不澄清、不追问、不输出 skill_id/intent/code/params
- current_user_query 是最高优先级事实
- 不伪造 image_id、file_id、mail_id 等资源句柄
- 缺主体/关键词/联系人等不影响语义帧输出

**下游使用**：后续所有节点（skill召回、intent召回、evaluator）都接收 `resolved_query` 和 `semantic_frame`，不再直接访问 `dialogue_history`。

### 3.3 Skill Candidate Generator — 一级候选召回

**输入**：
```json
{
    "resolved_query": "搜索最近的猫图片",
    "current_user_query": "最近的",
    "contextualized_request": {...},
    "available_skills": [15个SkillCard],
    "top_n": 3
}
```

**输出**：`SkillCandidateSet`
```python
class SkillCandidate:
    candidate_id: str          # "skill:mcloud_search_skill"
    skill_id: str | None       # "mcloud_search_skill" (None=普通对话)
    intent_domain: str         # "云盘搜索"
    confidence: float          # 0.9
    matched_cues: list[str]    # ["搜索", "图片", "猫"]
    risk_flags: list[str]      # []
    reason: str                # "query明确要求搜索资源"

class SkillCandidateSet:
    candidates: list[SkillCandidate]  # 默认Top-3
```

**后处理**（`_normalize_skill_candidates`）：
1. 按 skill_id 去重
2. 过滤掉不存在的 skill_id
3. 截断至 top_n 个业务候选
4. 追加"普通对话"兜底候选（skill_id=null）

**关键约束**：
- 强业务动作（搜索/查找/打开/生成/编辑等）必须至少给一个业务候选
- 答案型问句（是什么/为什么/怎么看/介绍下）可优先排普通对话
- 不输出未提供的 skill_id

### 3.4 Intent Candidate Generator — 二级候选召回

**执行方式**：对每个 skill candidate 分别调用模型，传入该 skill 的完整 Markdown。

**输入**：
```json
{
    "resolved_query": "搜索最近的猫图片",
    "current_user_query": "最近的",
    "contextualized_request": {...},
    "skill_candidate": {...},
    "skill_id": "mcloud_search_skill",
    "skill_markdown": "---\nname: 云盘搜索\n...\n### Tools Schema\n{...}",
    "top_m": 2
}
```

**输出**：`IntentCandidateSet`
```python
class IntentCandidate:
    candidate_id: str              # "mcloud_search_skill:搜图片:012:1"
    skill_id: str | None           # "mcloud_search_skill"
    skill_name: str | None         # "云盘搜索"
    intent: str                    # "搜图片"
    code: str                      # "012"
    params: dict[str, Any]         # {"timeList": ["最近"], "metadataList": ["猫"]}
    confidence: float              # min(skill_conf, intent_conf)
    matched_cues: list[str]        # 合并 skill_cues + intent_cues
    risk_flags: list[str]          # 合并 skill_risks + intent_risks
    reason: str
    alternatives: list[str]        # 同code的其他intent名
```

**后处理**（`_normalize_intent_candidate`）：
1. 校验 skill_id 存在
2. confidence 取 skill 和 intent 的 min
3. matched_cues 和 risk_flags 合并
4. candidate_id 按 `{skill_id}:{intent}:{code}:{rank}` 生成
5. 调用 `validate_intent_candidate` 本地校验

**候选集去重**（`_dedupe_intent_candidates`）：
- 按 route key `(skill_id, intent, code)` 去重
- 同 key 保留 confidence 最高的
- 普通对话候选（skill_id=null, code=000）作为兜底

### 3.5 Evaluator Reranker — 候选集重排序

**核心约束**：只能在候选集内选择或请求扩充，**不能自由生成新的 skill/intent/code**。

**输入**：
```json
{
    "resolved_query": "搜索最近的猫图片",
    "current_user_query": "最近的",
    "contextualized_request": {...},
    "available_skills": [15个SkillCard],
    "candidates": [IntentCandidate[], ...],
    "candidate_ids": ["mcloud_search_skill:搜图片:012:1", "ordinary_dialogue:000"],
    "expansion_round": 0
}
```

**输出**：`RerankDecision`
```python
class RerankDecision:
    verdict: Literal["select", "expand"]
    selected_candidate_id: str | None   # 选中的候选ID
    confidence: float                   # 评估置信度
    ranking: list[CandidateScore]       # 全量排序
    reason: str
    expand_scope: Literal[              # 仅 verdict=expand 时有效
        "skill_recall_gap",             # 一级skill方向漏召回
        "intent_recall_gap",            # skill对但intent候选漏召回
        "context_unclear",              # 仅记录风险，不触发行动
    ] | None
    expansion_hint: str | None          # 扩充提示
```

**关键约束**：
- 不评估参数完整性（缺参数/缺实体/缺句柄不降低候选）
- 不因 ontology_conflict 或 label_conflict 强行覆盖高置信候选
- 普通对话与业务候选冲突时，按 current_user_query 显式动作和 expected_result_type 判断

### 3.6 Switch Gate — 安全门控

**位置**：Evaluator 和最终输出之间

**触发条件**：Evaluator 选择的候选不是 Router 的第一位候选（`first_candidate`）

**处理逻辑**（`_apply_switch_gate`）：

```
if evaluator_action == "fallback":
    → 保留 first_candidate（evaluator输出无效）

if verdict == "expand":
    → 用综合评分选最优候选，记录 evaluator_action="expand"

if selected == first_candidate:
    → 直接输出，evaluator_action="select_top"

if selected != first_candidate:
    → 进入门控判断
```

**门控条件**（必须全部满足才允许切换）：

| 条件 | 阈值 | 含义 |
| --- | --- | --- |
| `rerank.confidence >= 0.85` | 0.85 | Evaluator 对自己的判断足够确信 |
| `margin >= 0.15` | 0.15 | 切换候选与首位候选的评分差足够大 |
| `has_route_critical_cue` | true | 目标候选的 matched_cues 在 query/semantic_frame 中有真实文本证据 |
| `no conflict risk flags` | true | 不存在 ontology_conflict 或 label_conflict |

**门控结果**：

| 结果 | evaluator_action | 含义 |
| --- | --- | --- |
| 允许切换 | `switch` | Evaluator 推荐被采纳 |
| 阻止切换 | `switch_blocked` | 保留首位候选，原因记录在 diagnostics |
| 首位一致 | `select_top` | Evaluator 和 Router 一致 |
| 扩充模式 | `expand` | 候选集不够好，综合评分选最优 |
| 无效评估 | `fallback` | Evaluator 输出不合法，回退到首位 |

**综合评分公式**（`_combined_score`，用于 expand 模式选最优）：

```
score = 0.45 * candidate.confidence
      + 0.45 * evaluator_score
      + 0.10 * cue_coverage_score
      - candidate_penalty
```

其中 `candidate_penalty` 对以下情况施加惩罚：
- 强业务动作却走普通对话（+0.2）
- function_skill 的通用入口（+0.1）
- current_query_conflict 风险标志（+0.3）
- ontology/label 冲突（+0.2）

### 3.7 Targeted Expansion — 定向候选扩充

**触发条件**：`rerank.verdict == "expand"` 且 `expansion_rounds < max_expansion_rounds`（默认1轮）

**两个扩充方向**：

#### skill_recall_gap（一级漏召回）

```
existing_skill_candidates + expansion_scope + expansion_hint
    → Skill Candidate Generator（重新召回）
    → 过滤出新 skill candidates
    → Intent Candidate Generator（为新 skill 生成 intent）
    → 合并到候选集
```

#### intent_recall_gap（二级漏召回）

```
从 rerank.ranking 中取 top-2 已有 skill
    → Intent Candidate Generator（在该 skill 内重新召回）
    → 过滤出新 intent candidates
    → 合并到候选集
```

**扩充后**：重新调用 Evaluator Rerank，再次进入 Switch Gate。

### 3.8 Validation — 本地硬校验

**`validate_intent_candidate`**（`validation.py`）：

```python
def validate_intent_candidate(registry, candidate):
    # 普通对话候选：必须是 intent="普通对话", code="000"
    if candidate.skill_id is None:
        assert candidate.intent == "普通对话"
        assert candidate.code == "000"

    # 业务候选：
    assert registry.has(candidate.skill_id)           # skill 存在
    skill = registry.get(candidate.skill_id)
    assert candidate.intent in skill.intents          # intent 在 skill 内
    assert candidate.code == skill.intents[candidate.intent].code  # code 匹配
```

**`validate_rerank_decision`**（`validation.py`）：

```python
def validate_rerank_decision(decision, candidate_ids):
    # ranking 中的 candidate_id 必须都在候选集内
    # ranking 中不能有重复 candidate_id
    # verdict=select 时必须有 selected_candidate_id 且在候选集内
    # verdict=expand 时必须有 expand_scope
```

### 3.9 RouteResult — 最终输出

```python
class RouteResult:
    status: Literal["matched", "clarify", "no_match"]
    skill: SkillRef | None           # {"id": "mcloud_search_skill", "name": "云盘搜索"}
    intent: str | None               # "搜图片"
    code: str | None                 # "012"
    params: dict[str, Any]           # {"timeList": ["最近"], "metadataList": ["猫"]}
    confidence: float                # 0.9
    reason: str | None
    resolved_query: str              # "搜索最近的猫图片"
    context_relation: str            # "continuation"
    visited_skills: list[str]        # 本轮访问过的所有 skill
    loop_count: int                  # 1 + expansion_rounds
    correction_scopes: list[str]     # 经历的扩充方向
    alternatives: list[IntentCandidate]  # 未选中的候选
    diagnostics: RouteDiagnostics    # 路由诊断信息
```

### 3.10 双路径路由

系统提供两条路由路径：

| 路径 | 方法 | 链路 | 适用场景 |
| --- | --- | --- | --- |
| 完整链路 | `route()` | Contextualizer → Skill → Intent → Evaluator → Switch Gate → 输出 | 追求准确率 |
| 快捷路径 | `route_no_loop()` | Contextualizer → Skill → Intent → 直接输出首位候选 | 追求低延迟 |

快捷路径跳过 Evaluator Rerank、Switch Gate 和定向扩充，直接返回候选集第一位。在评测中用于对比 Loop 是否有增益。

### 3.11 多轮对话管理

`IntentDialogueAgent`（`dialogue.py`）封装有状态对话：

```python
class IntentDialogueAgent:
    def __init__(self, router: IntentRouter):
        self.router = router
        self.history = DialogueHistory()  # turns: list[DialogueTurn]

    async def send(self, query) -> RouteResult:
        result = await self.router.route(query, dialogue_history=self.history)
        self.history.turns.append(DialogueTurn(
            user_query=query,
            result=summarize_route_result(result),  # 摘要存储
        ))
        return result
```

**历史存储策略**：
- 每轮存储 `DialogueRouteSummary`（status/skill_id/intent/code/params/resolved_query）
- 历史只注入最近 5 轮（`DIALOGUE_HISTORY_LIMIT = 5`）
- 不注入业务执行结果、no_match 失败原因
- 历史 matched 结果可用于理解"它/这个/第二个"等指代

---

## 四、与旧版（v1）的核心差异

### 4.1 架构对比

```
旧版 v1（串联纠错循环）：
  User Query → Skill Route → Intent Route → Validation → Evaluator
                                                       ├─ accept → 输出
                                                       ├─ clarify → 反问用户
                                                       └─ reject → 分层重试
                                                          ├─ skill_mismatch → 重新选skill
                                                          ├─ intent_mismatch → 锁定skill重选intent
                                                          └─ param_mismatch → 锁定skill+intent修参数

新版 v2.0.0（候选集择优+补齐）：
  User Query → Contextualizer → Skill Candidates → Intent Candidates
                                                          ↓
                                                  Candidate Set
                                                          ↓
                                                  Evaluator Rerank
                                                  ├─ select → Switch Gate → 输出
                                                  └─ expand → 定向扩充 → 重新Rerank
```

### 4.2 关键差异矩阵

| 维度 | 旧版 v1 | 新版 v2.0.0 |
| --- | --- | --- |
| **核心范式** | 单点预测 + 事后纠错 | 候选集生成 + 择优 |
| **路由决策** | 串行确定：先选一个skill，再选一个intent | 并行候选集：多个skill×多个intent同时进入候选池 |
| **Evaluator** | 三态：accept / clarify / reject | 二态：select / expand |
| **Evaluator能力** | 可自由判断对错，输出 reject + reject_scope | 只能在候选集中选择，不能发明新code |
| **纠错机制** | 分层重试循环（skill错→重选skill，intent错→重选intent，参数错→修参数） | 定向候选扩充（最多1轮，补漏召回方向） |
| **不确定性处理** | Evaluator输出clarify，含question/options | 始终输出最稳妥候选，不向用户澄清 |
| **安全控制** | Evaluator直接决定最终结果 | Switch Gate校验后才允许切换 |
| **Contextualizer** | ❌ 无，各节点分别解释历史 | ✅ 统一预处理，resolved_query + semantic_frame |
| **参数验证** | 作为阻塞条件（params不合法触发重试） | 不阻塞路由（缺参数/缺实体/缺句柄不影响code选择） |
| **普通对话** | 作为terminal no_match | 作为正常候选（code=000, skill_id=null） |
| **对比评测** | 无 | 内置compare_no_loop双路径对比 |

### 4.3 设计理念转变

**1. 从"纠错"到"补齐"**

旧版假定模型第一次预测可能出错，需要 Evaluator 发现错误后触发重试。但重试时模型获得的信息不变（仍是同一 query），只是多了一段"上次错了"的提示——这本质上是让模型猜"上次哪里错了"，而不是给它新的判断依据。

新版改为先让模型列出多个可能方向（候选集），再用 Evaluator 从中选择。如果候选集不够好，定向补充遗漏方向。核心转变是：**与其让模型猜自己哪里错了，不如让它把可能的方向都列出来。**

**2. 从"三态"到"二态"**

旧版 Evaluator 有三种动作：accept（通过）、clarify（不确定）、reject（拒绝）。clarify 和 reject 的语义边界模糊——什么算"不确定"vs"明确错误"？模型自报的置信度不可靠，需要二次阈值判断。

新版简化为两种：select（候选集中有答案）或 expand（候选集缺少方向）。消除了 clarify 这一模糊状态，系统始终输出决策。

**3. 新增 Contextualizer 统一入口**

旧版中，对话历史由各节点（skill路由、intent路由、evaluator）分别解释，容易出现不一致——skill路由认为用户在"延续"，intent路由认为用户在"改口"。

新版将对话历史归一化前置为一个独立的 Contextualizer 节点，输出统一的 resolved_query 和 semantic_frame，后续所有节点共享同一份语义理解。

**4. 新增 Switch Gate 安全层**

旧版中，Evaluator 的判断直接决定最终结果。如果 Evaluator 出现误判（将正确结果误判为错误），Router 会被迫重试并可能输出更差的结果。

新版在 Evaluator 和最终输出之间增加 Switch Gate，只有满足严格条件（confidence≥0.85、margin≥0.15、有真实证据词、无冲突风险）才允许 Evaluator 切换候选。不满足条件时，切换被阻止，保留首位候选。

**5. 消除 clarify 机制**

旧版中，系统不确定时向用户反问。但"不确定"的判断本身是一个难题——模型经常在可以决策时也输出 clarify。

新版中，系统始终预测最可能的 intent/code，不输出 clarify。路由理念是"预测最可能的 skill/intent/code"，缺失实体、资源句柄、操作对象、参数不触发澄清。

---

## 五、评测体系

### 5.1 单元测试

使用 `FakeStructuredClient` 模拟模型输出，覆盖核心场景：

- 正常匹配链路
- Evaluator 切换候选（门控通过/阻止）
- 定向扩充（skill_recall_gap / intent_recall_gap）
- 快捷路径（route_no_loop）
- Contextualizer 多轮对话
- 空候选回退到普通对话

### 5.2 Excel 多轮评测

| 评测命令 | 说明 |
| --- | --- |
| `eval-xlsx` | 旧格式评测（"第N轮对话"/"第N轮预期意图"） |
| `eval-format-xlsx` | 新格式评测（"上文N"/"上文意图N"） |

**两种评测模式**：

| 模式 | 历史构造方式 | 用途 |
| --- | --- | --- |
| `gold_history` | 使用表格中的期望意图构造历史 | 隔离评测当前轮路由能力 |
| `end2end` | 按端到端多轮对话模拟执行历史轮次 | 评测含历史累积误差的真实效果 |

**双路径对比**（`--compare-no-loop`）：
- 同一用例同时执行 `route()` 和 `route_no_loop()`
- 输出 `both_passed` / `both_failed` / `loop_only_passed` / `no_loop_only_passed`
- 直接量化 Loop 的净收益/净损失

### 5.3 评测指标

| 指标 | 说明 |
| --- | --- |
| accuracy | 最终准确率 |
| first_candidate_accuracy | 首位候选准确率（无Loop基线） |
| top_n_recall | 候选集中是否包含正确答案 |
| evaluator_switch_gain | Evaluator切换带来的正收益 |
| evaluator_switch_loss | Evaluator切换带来的负收益 |
| evaluator_switch_net_gain | 切换净收益 |
| expansion_gain | 扩充带来的正收益 |
| avg_loop_count | 平均Loop次数 |
| avg_elapsed_ms | 平均耗时 |

---

## 六、技术栈

| 组件 | 技术 |
| --- | --- |
| 语言 | Python 3.11+ |
| Agent框架 | AgentScope 2.x |
| 模型 | DashScope (qwen-plus) / OpenAI 兼容 |
| 数据校验 | Pydantic v2 |
| 配置格式 | Markdown + YAML Frontmatter |
| 测试 | pytest + pytest-asyncio |
| 评测 | openpyxl (Excel) |