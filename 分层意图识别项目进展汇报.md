# 基于 Agent Loop 的意图路由方案探索

汇报日期：2026-06-29

---

## 一、项目背景

移动云盘意图路由的核心任务：给定用户自然语言输入，输出结构化的路由决策（skill / intent / code / params），供下游业务工具执行。这是一个典型的 NLU 分类+抽取问题。

当前单次模型调用的准确率约为 80%——剩余 20% 的错误率是工程上线的实质障碍。本项目的核心命题是：**能否通过 Agent Loop 机制，在不引入外部信息的前提下，将意图路由的准确率从单次预测的基线提升到可上线水平？**

---

## 二、Agent Loop 方案的设计动机

### 2.1 为什么借鉴 Agent Loop

Agent Loop 在以下领域展现了显著的迭代增益：

- **代码生成**（Devin、Cursor）：模型生成代码 → 编译器反馈错误 → 模型修正 → 循环至通过
- **工具调用 Agent**：模型调用工具 → 获取外部数据 → 基于新数据调整下一步
- **RLHF 对齐**：模型生成 → 人类反馈 → 模型修正偏好

这些场景的共同模式是：

```
模型行动 → 外部环境反馈 → 模型基于新信息调整 → 循环
```

我们的直觉假设是：**能否将"外部环境反馈"替换为"另一个模型（Evaluator）的反馈"，构造一个闭环的意图路由 Loop？**

### 2.2 两个版本的 Loop 设计

基于以上假设，我们设计并实现了两个版本的 Loop 架构：

**v1：串联纠错循环（Predict → Judge → Retry）**

```
用户输入 → Router(一次预测) → Evaluator(判断对错)
                              ├─ accept → 输出
                              ├─ clarify → 反问用户
                              └─ reject → 分层重试(skill错→重选skill, intent错→重选intent, 参数错→修参数)
```

核心思想：Evaluator 扮演"外部环境"角色，对 Router 的输出进行判断，Router 根据判断结果修正。

**v2：候选集择优+定向补齐（Generate → Select → Expand）**

```
用户输入 → Contextualizer(语义归一)
       → Router(Top-N候选集生成)
       → Evaluator(候选集中选择最优)
       ├─ select → Switch Gate(安全门控) → 输出
       └─ expand → 定向扩充候选集 → 重新Rerank
```

核心思想：降低 Evaluator 的自由度，约束其只能在候选集中选择，减少"纠偏"风险。

---

## 三、实验结论

经过两周系统测试，两个版本的 Loop 方案**均未展现出对单次预测基线的正向增益**。

关键实验数据：

| 指标 | 单次预测（基线） | v1 串联纠错 | v2 候选集择优 |
| --- | --- | --- | --- |
| 准确率 | 基线 | 低于基线 | 与基线持平或略低 |
| Evaluator 正纠（纠错成功） | — | 偶有发生 | 偶有发生 |
| Evaluator 误纠（纠偏为错） | — | 频繁发生 | 由 Switch Gate 抑制，但未转化为增益 |
| Loop 触发率 | — | 较高 | 较低（Switch Gate 拦截） |
| 平均耗时 | 1× | 1.5-3× | 1.5-2.5× |

核心现象：**Loop 增加的模型调用带来了额外的推理噪声，但未带来额外的有效信息，导致噪声增益超过或抵消了纠错增益。**

---

## 四、第一性原理分析：为什么 Agent Loop 在意图路由中失效

### 4.1 成功的 Agent Loop 的必要条件：开放信息通道

分析所有 Agent Loop 成功的场景，其共同特征是**每次迭代有外部新信息注入**：

| 场景 | 信息源 | 每次迭代的新信息 |
| --- | --- | --- |
| 代码生成 | 编译器/运行时 | 编译错误、测试结果、运行时输出 |
| 工具调用 | API/数据库 | 外部数据和状态 |
| Web 搜索 | 互联网 | 新检索到的文档 |
| 游戏博弈 | 游戏环境 | 对手行动后的新状态 |
| RLHF | 人类标注 | 人类偏好判断 |

这些场景的 Loop 是**开放的**——信息从外部流入，每次迭代都缩小不确定性。

意图路由的 Loop 是**封闭的**——所有信息都包含在用户原始 query 中。Evaluator 的"反馈"只是同一信息源的另一种模型解读，不是新信息。

```
开放 Loop： 信息(用户query) → 行动 → 新信息(环境反馈) → 修正 → 收敛
封闭 Loop： 信息(用户query) → 行动 → 同源解读 → 再行动 → 发散/震荡
```

### 4.2 数学分析：噪声的此消彼长

假设单次模型调用（Router）的准确率为 $p$，Evaluator 的准确率为 $q$。在一次 Loop 中：

| Router 输出 | Evaluator 判断 | 最终结果 | 概率 |
| --- | --- | --- | --- |
| 正确 | 认为正确 | ✅ 正确 | $p \cdot q$ |
| 正确 | 认为错误 | ❌ 被误纠 | $p \cdot (1-q)$ |
| 错误 | 认为正确 | ❌ 未被纠正 | $(1-p) \cdot q$ |
| 错误 | 认为错误 | ✅ 被纠正 | $(1-p) \cdot (1-q)$ |

Loop 后的准确率：

$$P_{loop} = p \cdot q + (1-p) \cdot (1-q)$$

Loop 收益 = $P_{loop} - p$：

$$P_{loop} - p = (1-p)(1-q) - p(1-q) = (1-q)(1-2p)$$

**关键结论：当 $p > 0.5$ 时，$P_{loop} - p < 0$（只要 $q < 1$）。**

即：**只要 Router 的准确率超过 50%，且 Evaluator 不是完美的（q < 1），Loop 必然降低准确率。**

以实际数据代入：若 Router 准确率 $p = 0.8$，Evaluator 准确率 $q = 0.8$：

$$P_{loop} = 0.8 \times 0.8 + 0.2 \times 0.2 = 0.64 + 0.04 = 0.68$$

Loop 后准确率从 80% **降至 68%**。即使 Evaluator 准确率达到 90%，$P_{loop} = 0.72 + 0.02 = 0.74$，仍然低于原始 80%。

**只有当 $p < 0.5$ 时 Loop 才有正收益。但一个准确率低于 50% 的系统本身就不具备上线条件，Loop 也无法将其提升到可用水平。**

### 4.3 相关性陷阱：同模型同偏见

上述分析假设 Router 和 Evaluator 的错误是**独立**的。实际情况更差——它们使用相同或相似的模型，错误高度相关：

- 同一模型对同一输入的困难案例（歧义、边界情况）会同时犯错
- Evaluator 对 Router 的**系统性错误是盲的**——它自己也倾向于犯同样的错误
- 这意味着 $(1-p) \cdot q$（错误未被纠正）的概率远高于独立假设

```
错误独立性假设：Router 错 ≠ Evaluator 错 → 有机会纠正
实际情况：     Router 错 ≈ Evaluator 也错 → 纠正不了
```

### 4.4 信息论极限：用户的 query 中没有更多信息了

从信息论角度，用户的原始 query 包含的信息量是固定的。设 query 的语义信息量为 $I(Q)$：

- 单次模型调用：提取 $I(Q)$ 中的信息做决策
- Loop 第一次迭代：模型重新读取 $I(Q)$ 加上自己上次的解读
- Loop 第 N 次迭代：模型重新读取 $I(Q)$ 加上自己前 N-1 次的解读

**模型自己的解读不包含超出 $I(Q)$ 的新信息。** 它只是对同一信息的重新排列。这类似于：

> 反复阅读同一段文字，不会让你理解得更多——你只是在用不同的方式复述你已经理解（或误解）的内容。

Agent Loop 在其他场景成功，是因为外部环境反馈注入了 $I(E_t)$——第 t 轮的环境新信息。而意图路由的 $I(E_t) = 0$（无外部环境），Loop 的马尔可夫链退化为：

$$X_{t+1} = f(X_t, Q)$$

其中 $X_t$ 是第 t 轮的模型输出，$Q$ 是固定的用户 query。这是一个**无外部驱动的自回归过程**，要么收敛到不动点（Evaluator 总是 accept），要么震荡（Evaluator 来回改），但不会持续向正确方向收敛。

### 4.5 总结：Loop 失效的本质原因

```
Agent Loop 有效的充要条件：每次迭代有外部新信息注入，即 I(E_t) > 0

意图路由的约束：I(E_t) = 0（无工具调用、无环境交互、无人类反馈）

结论：在封闭信息系统中，多模型 Loop 不会带来准确率增益，反而因噪声叠加而降低。
```

这不是工程实现问题，而是**信息论层面的根本性约束**。无论怎么调整 Loop 的架构（串联纠错、候选集择优、Switch Gate 门控），都无法绕过"没有新信息进入系统"这一基本事实。

---

## 五、关键发现与启示

### 5.1 已验证的结论

1. **多模型串联 Loop 在意图路由中不可行**：两个版本、两周实验、数学推导，三方面都指向同一结论。

2. **Switch Gate 能有效"止损"但不能"增益"**：v2 的 Switch Gate 成功阻止了大部分误纠，将准确率拉回到接近基线，但无法超越基线——它只是减少了伤害，没有创造价值。

3. **Evaluator 自报置信度不可靠**：模型对自身判断的置信度与实际正确率相关性弱，不能作为决策依据。

### 5.2 方法论启示

1. **"Agent Loop 有效"不是普适规律**：Loop 的有效性取决于是否有外部信息注入，而非 Loop 本身的结构。

2. **设计前先分析信息流**：在投入工程实现之前，先回答"每次迭代的新信息从哪里来"——如果答案是没有，Loop 方案大概率不可行。

3. **消除一个方向本身就是进展**：我们排除了"封闭 Loop"这一整类方案，避免了团队在未来继续在这个方向上投入，这是探索性工作的核心价值。

---

## 六、工程产出

尽管方案未达到预期效果，项目仍产出了可复用的工程基础设施：

| 产出 | 说明 | 复用价值 |
| --- | --- | --- |
| 15 类 Skill 能力配置 | Markdown 格式的 Special Rules + Tools Schema | 后续任何方案的基础配置 |
| SkillRegistry | 能力注册表，加载/解析/查询 | 已稳定，可直接复用 |
| Contextualizer | 多轮对话语义归一（resolved_query + semantic_frame） | 多轮对话的必要预处理 |
| 双路径路由框架 | route() 完整链路 + route_no_loop() 快捷路径 | 可快速替换内部策略 |
| Excel 多轮评测体系 | gold_history / end2end / 双路径对比 | 后续方案的效果度量工具 |
| Switch Gate 安全门控 | 多条件校验的切换决策机制 | 可复用于任何需要安全控制的决策场景 |
| 单元测试 | FakeStructuredClient 模拟模型输出 | 快速验证新策略 |

---

## 七、下一步方向

基于"信息注入是 Loop 有效的必要条件"这一结论，后续方案应考虑：

1. **引入外部信息源**：让 Loop 真正"开放"——例如接入知识库检索、同义词扩展、用户画像等外部信号，使每次迭代有真正的新信息。

2. **单次预测精度提升**：既然封闭 Loop 无法增益，资源应集中在提升单次预测的准确率——优化 prompt、扩充规则、增加 few-shot 示例。

3. **人机协同**：对低置信度 case 走人工审核而非模型间互审，人类反馈是真正的外部信息。

4. **规则兜底**：对高频错误 case 用硬规则覆盖，而非依赖模型 Loop 修正。

---

## 八、技术栈

- **语言**：Python 3.11+
- **Agent 框架**：AgentScope 2.x
- **模型**：DashScope（qwen-plus） / OpenAI 兼容接口
- **数据校验**：Pydantic v2
- **配置格式**：Markdown + YAML Frontmatter
- **测试框架**：pytest + pytest-asyncio
- **评测工具**：openpyxl